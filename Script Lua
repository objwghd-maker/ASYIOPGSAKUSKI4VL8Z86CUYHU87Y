local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer

LocalPlayer.CameraMode = Enum.CameraMode.Classic
LocalPlayer.CameraMinZoomDistance = 0.5
LocalPlayer.CameraMaxZoomDistance = 128

if CoreGui:FindFirstChild("MainHubUI") then
    CoreGui.MainHubUI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MainHubUI"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

local ToggleMenuBtn = Instance.new("TextButton")
ToggleMenuBtn.Name = "ToggleMenuBtn"
ToggleMenuBtn.Size = UDim2.new(0, 70, 0, 32)
ToggleMenuBtn.Position = UDim2.new(0.02, 0, 0.2, 0)
ToggleMenuBtn.BackgroundColor3 = Color3.fromRGB(20, 24, 32)
ToggleMenuBtn.Text = "MENU"
ToggleMenuBtn.TextColor3 = Color3.fromRGB(0, 230, 255)
ToggleMenuBtn.TextSize = 11
ToggleMenuBtn.Font = Enum.Font.GothamBold
ToggleMenuBtn.Active = true
ToggleMenuBtn.Draggable = true
ToggleMenuBtn.Parent = ScreenGui

local OpenCorner = Instance.new("UICorner")
OpenCorner.CornerRadius = UDim.new(0, 6)
OpenCorner.Parent = ToggleMenuBtn

local OpenStroke = Instance.new("UIStroke")
OpenStroke.Color = Color3.fromRGB(0, 230, 255)
OpenStroke.Thickness = 2
OpenStroke.Parent = ToggleMenuBtn

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 220, 0, 240)
MainFrame.Position = UDim2.new(0.1, 0, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 24, 32)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(0, 230, 255)
MainStroke.Thickness = 2
MainStroke.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "TitleLabel"
TitleLabel.Size = UDim2.new(1, -30, 0, 30)
TitleLabel.Position = UDim2.new(0, 10, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "MAIN MENU"
TitleLabel.TextColor3 = Color3.fromRGB(0, 230, 255)
TitleLabel.TextSize = 12
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = MainFrame

local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Size = UDim2.new(0, 25, 0, 25)
CloseBtn.Position = UDim2.new(1, -28, 0, 3)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
CloseBtn.TextSize = 12
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = MainFrame

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

ToggleMenuBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = MainFrame
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 6)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local HeaderPadding = Instance.new("Frame")
HeaderPadding.Size = UDim2.new(1, 0, 0, 32)
HeaderPadding.BackgroundTransparency = 1
HeaderPadding.LayoutOrder = 0
HeaderPadding.Parent = MainFrame

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleBtn"
ToggleBtn.Size = UDim2.new(0, 200, 0, 36)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(30, 35, 45)
ToggleBtn.Text = "วิ่งเร็ว 50: OFF"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
ToggleBtn.TextSize = 11
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.LayoutOrder = 1
ToggleBtn.Parent = MainFrame

local UICornerSpeed = Instance.new("UICorner")
UICornerSpeed.CornerRadius = UDim.new(0, 6)
UICornerSpeed.Parent = ToggleBtn

local UIStrokeSpeed = Instance.new("UIStroke")
UIStrokeSpeed.Color = Color3.fromRGB(0, 230, 255)
UIStrokeSpeed.Thickness = 1
UIStrokeSpeed.Parent = ToggleBtn

local speedEnabled = false
local defaultSpeed = 16
local targetSpeed = 50
local speedConnection = nil

local function applySpeed()
    if speedEnabled then
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.WalkSpeed = targetSpeed
            end
        end
    end
end

ToggleBtn.MouseButton1Click:Connect(function()
    speedEnabled = not speedEnabled
    
    if speedEnabled then
        ToggleBtn.Text = "วิ่งเร็ว 50: ON"
        ToggleBtn.TextColor3 = Color3.fromRGB(80, 255, 120)
        speedConnection = RunService.Heartbeat:Connect(applySpeed)
    else
        ToggleBtn.Text = "วิ่งเร็ว 50: OFF"
        ToggleBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
        if speedConnection then
            speedConnection:Disconnect()
            speedConnection = nil
        end
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.WalkSpeed = defaultSpeed
            end
        end
    end
end)

local TeleportBtn = Instance.new("TextButton")
TeleportBtn.Name = "TeleportBtn"
TeleportBtn.Size = UDim2.new(0, 200, 0, 36)
TeleportBtn.BackgroundColor3 = Color3.fromRGB(30, 35, 45)
TeleportBtn.Text = "ไปที่ประตู"
TeleportBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
TeleportBtn.TextSize = 11
TeleportBtn.Font = Enum.Font.GothamBold
TeleportBtn.LayoutOrder = 2
TeleportBtn.Parent = MainFrame

local TeleportCorner = Instance.new("UICorner")
TeleportCorner.CornerRadius = UDim.new(0, 6)
TeleportCorner.Parent = TeleportBtn

local TeleportStroke = Instance.new("UIStroke")
TeleportStroke.Color = Color3.fromRGB(0, 230, 255)
TeleportStroke.Thickness = 1
TeleportStroke.Parent = TeleportBtn

TeleportBtn.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        char.HumanoidRootPart.CFrame = CFrame.new(-36.11, 3.90, 111.03)
    end
end)

local TpBtn1 = Instance.new("TextButton")
TpBtn1.Name = "TpBtn1"
TpBtn1.Size = UDim2.new(0, 200, 0, 36)
TpBtn1.BackgroundColor3 = Color3.fromRGB(30, 35, 45)
TpBtn1.Text = "ไปที่ห้องพยาบาล"
TpBtn1.TextColor3 = Color3.fromRGB(255, 255, 255)
TpBtn1.TextSize = 11
TpBtn1.Font = Enum.Font.GothamBold
TpBtn1.LayoutOrder = 3
TpBtn1.Parent = MainFrame

local UICorner1 = Instance.new("UICorner")
UICorner1.CornerRadius = UDim.new(0, 6)
UICorner1.Parent = TpBtn1

local UIStroke1 = Instance.new("UIStroke")
UIStroke1.Color = Color3.fromRGB(0, 230, 255)
UIStroke1.Thickness = 1
UIStroke1.Parent = TpBtn1

TpBtn1.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        char.HumanoidRootPart.CFrame = CFrame.new(-5.36, 3.98, -9.85)
    end
end)

local TpBtn2 = Instance.new("TextButton")
TpBtn2.Name = "TpBtn2"
TpBtn2.Size = UDim2.new(0, 200, 0, 36)
TpBtn2.BackgroundColor3 = Color3.fromRGB(30, 35, 45)
TpBtn2.Text = "ไปที่ลงทะเบียนผู้ป่วย"
TpBtn2.TextColor3 = Color3.fromRGB(255, 255, 255)
TpBtn2.TextSize = 11
TpBtn2.Font = Enum.Font.GothamBold
TpBtn2.LayoutOrder = 4
TpBtn2.Parent = MainFrame

local UICorner2 = Instance.new("UICorner")
UICorner2.CornerRadius = UDim.new(0, 6)
UICorner2.Parent = TpBtn2

local UIStroke2 = Instance.new("UIStroke")
UIStroke2.Color = Color3.fromRGB(0, 230, 255)
UIStroke2.Thickness = 1
UIStroke2.Parent = TpBtn2

TpBtn2.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        char.HumanoidRootPart.CFrame = CFrame.new(16.99, 3.98, 110.50)
    end
end)

